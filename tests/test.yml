# test playbook

- hosts: localhost
  connection: local
  become: true
  roles:
    - {role: ../roles/user}
    - {role: ../roles/common}
    - {role: ../roles/audio-cli}
    - {role: ../roles/audio-gui}
    - {role: ../roles/backup-restore}
    - {role: ../roles/chrome}
    - {role: ../roles/db-server}
    - {role: ../roles/dev-common}
    - {role: ../roles/dev-gui}
    - {role: ../roles/dev-ansible}
    - {role: ../roles/dev-go}
    - {role: ../roles/dev-kubernetes}
    - {role: ../roles/dev-python}
    - {role: ../roles/dev-ruby}
    - {role: ../roles/docker}
    # excluding file-server role for now as downloading Dropbox Headless fails frequently due to issue on Dropbox side
    #- {role: ../roles/file-server}
    - {role: ../roles/gaming}
    # exlucing gui-common role for now as downloading Simplenote fails frequently due to "error 104 connection reset by peer"
    #- {role: ../roles/gui-common}
    - {role: ../roles/gui-i3}
    - {role: ../roles/gui-xfce}
    - {role: ../roles/monitoring}
    - {role: ../roles/office}
    - {role: ../roles/repo-extra}
    - {role: ../roles/web-server}
    - {role: ../roles/unattended-upgrades}
    - {role: ../roles/vagrant}
    - {role: ../roles/virt-server}
    - {role: ../roles/vm}
    # explicitly including hardening last since its a WIP and might screw up connectivity to the test box
    - {role: ../roles/hardening}
