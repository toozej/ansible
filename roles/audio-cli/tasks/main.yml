---
- name: install latest version of ffmpeg
  package:
    name: ffmpeg
    state: latest
  # temporarily ignore errors due to Fedora + RPMFusion repo conflicts
  ignore_errors: true

- name: install latest version of mediainfo
  package:
    name: mediainfo
    state: latest

- name: install latest version of mkvtoolnix
  package:
    name: mkvtoolnix
    state: latest
